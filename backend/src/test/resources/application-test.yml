spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:bu_management_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;LOCK_TIMEOUT=30000
    username: sa
    password:
  flyway:
    enabled: false

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
